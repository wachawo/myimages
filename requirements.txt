# Full dependency set for myimages: runtime + optional features + development.
#
# pyproject.toml is the source of truth. This file mirrors it, so every version
# constraint below must stay identical to the one declared there; update both
# together or they will silently disagree.
#
# This installs dependencies only, not the myimages package itself. To get the
# `myimages` entry point as well, also run `pip install -e .` (or `make install`).

# Runtime ([project.dependencies]) - always required.
PySide6>=6.6
Pillow>=10.0

# Optional features ([project.optional-dependencies]).
# The code imports these lazily and degrades gracefully when they are absent,
# but they are listed here so a single install enables every pip-installable
# feature.
Send2Trash>=1.8      # "trash" extra: delete to system trash instead of unlink
pillow-heif>=0.16    # "heif" extra: open and convert Apple HEIC photos
#
# FFmpeg powers video trim/crop/scale and GIF export. It is a system tool, not a
# pip package, so it cannot be listed here: install it with your package manager
# (e.g. `sudo apt install ffmpeg`). The app detects it at runtime.

# Development ("dev" extra) - linting, typing, hooks and the test suite.
black>=24.0
ruff>=0.6
mypy>=1.11
pre-commit>=3.7
pytest>=8.0
pytest-cov>=5.0
pytest-qt>=4.4
