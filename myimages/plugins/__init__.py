"""Bundled example plugins.

Each ``*.py`` file here (and in the user plugins folder) may define a
``register(registry)`` function that adds viewers for extra file types. See
``three_d_preview.py`` for a minimal, dependency-free example.
"""
