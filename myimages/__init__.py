"""myImages — a lightweight photo and video viewer, editor and converter."""

from __future__ import annotations

__version__ = "0.0.2"
APP_NAME = "myImages"
ORGANISATION = "myImages"

# The freedesktop identifier: the .desktop file's basename, the icon-theme name,
# and what the window tells a panel it is. One literal, because the icon only
# appears in the taskbar when all three agree.
DESKTOP_ID = "myimages"
